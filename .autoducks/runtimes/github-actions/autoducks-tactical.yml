name: 'Autoducks: Tactical Agent'

on:
  issue_comment:
    types: [created]

permissions:
  contents: read
  issues: write
  pull-requests: write
  id-token: write
jobs:
  tactical:
    if: >-
      github.event.issue.pull_request == null &&
      (startsWith(github.event.comment.body, '/agents devise') ||
       (startsWith(github.event.comment.body, '/agents execute') &&
        contains(github.event.issue.labels.*.name, 'Feature') &&
        !contains(github.event.issue.labels.*.name, 'Ready')))
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Load agent defaults
        id: agent
        run: AUTODUCKS_AGENT=tactical bash .autoducks/core/config/load-agent-defaults.sh >> "$GITHUB_OUTPUT"
      - name: Parse directive
        id: directive
        run: |
          source .autoducks/core/config/parse-directive.sh >> "$GITHUB_OUTPUT"
        env:
          COMMENT_BODY: ${{ github.event.comment.body }}
      - name: Pre-execution
        run: bash .autoducks/agents/tactical/pre.sh
        env:
          ISSUE_NUM: ${{ github.event.issue.number }}
          REPO: ${{ github.repository }}
          COMMENT_ID: ${{ github.event.comment.id }}
          RUN_ID: ${{ github.run_id }}
          COMMENTER: ${{ github.event.comment.user.login }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Run LLM agent
        uses: ./.autoducks/providers/llm/claude
        with:
          prompt_file: .autoducks/agents/tactical/prompt.md
          allowed_tools: "Read,Write,Glob,Grep"
          model: ${{ steps.directive.outputs.model || steps.agent.outputs.model }}
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
      - name: Post-execution
        if: always()
        run: bash .autoducks/agents/tactical/post.sh
        env:
          ISSUE_NUM: ${{ github.event.issue.number }}
          REPO: ${{ github.repository }}
          COMMENT_ID: ${{ github.event.comment.id }}
          RUN_ID: ${{ github.run_id }}
          COMMENTER: ${{ github.event.comment.user.login }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
